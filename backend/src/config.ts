/**
 * Contains all of the configuration.
 * Hard coded at the moment - will probably be refactored
 * to use environment variables or json files as source.
 */

 export const dbConnectionHost = 'localhost';

 export const dbConnectionPort = 3306;

 export const dbConnectionUser = 'root';

 export const dbConnectionPassword = 'secret';

 export const dbConnectionDB = 'vale';

 export const dbConnectionLimit = 10;